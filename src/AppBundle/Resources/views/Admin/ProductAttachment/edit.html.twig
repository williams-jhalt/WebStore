{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block admin_body %}

    {{ include('AppBundle:Admin/ProductAttachment:nav.html.twig') }}
    
    <img src="{{ path('display_product_image', {path: productAttachment.path, width: 80, height: 80}) }}">

    {{ form(form) }}

    <p><a class="btn btn-danger" href="{{ path('admin_product_list', app.request.query.all) }}">Cancel</a></p>

{% endblock %}
